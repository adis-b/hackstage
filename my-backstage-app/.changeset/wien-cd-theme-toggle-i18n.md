---
"@stadt-wien/backstage-plugin-cd": patch
---

Use standard AppTheme ids `light`/`dark` with empty titles so Settings → Appearance theme buttons follow `userSettings` translations (fixes German labels showing when UI language is English).
