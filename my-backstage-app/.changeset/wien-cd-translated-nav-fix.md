---
"@stadt-wien/backstage-plugin-cd": patch
---

Fix `nav-content:app/translated-nav` startup error: pass `params.component` to `NavContentBlueprint.make` (required output `core.nav-content.component`).
