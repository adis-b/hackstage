# @wien/backstage-instanceswitcher-plugin

## 1.0.0

Initial release under `@wien` scope. Split from `@stadt-wien/backstage-plugin-cd`.

- Floating instance switcher (`app-root-element:instanceswitcher/instance-switcher`)
- Own translation ref `wienInstanceSwitcherTranslationRef`
- Depends on `@wien/backstage-cd-plugin` for variant colours

**Breaking:** extension id changed from `app-root-element:wien-cd/instance-switcher`.
